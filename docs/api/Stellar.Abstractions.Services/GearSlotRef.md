# GearSlotRef structure

One equipped item from social data: which item-config id occupies which slot (no instance rolls — social data carries slot+id only).

```csharp
public struct GearSlotRef : IEquatable<GearSlotRef>
```

| parameter | description |
| --- | --- |
| Slot | Equipment slot code (wire `EquipNine.slot`). |
| EquipId | Item-config id occupying the slot (resolves name/quality/icon via the item table). |

## Public Members

| name | description |
| --- | --- |
| [GearSlotRef](GearSlotRef/GearSlotRef.md)(…) | One equipped item from social data: which item-config id occupies which slot (no instance rolls — social data carries slot+id only). |
| [EquipId](GearSlotRef/EquipId.md) { get; set; } | Item-config id occupying the slot (resolves name/quality/icon via the item table). |
| [Slot](GearSlotRef/Slot.md) { get; set; } | Equipment slot code (wire `EquipNine.slot`). |

## See Also

* namespace [Stellar.Abstractions.Services](../Stellar.Abstractions.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stellar.Abstractions.dll -->
