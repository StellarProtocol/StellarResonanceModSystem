# ConditionalElement record

React-style conditional. Both subtrees are built once; the renderer SetActive-toggles them each refresh from *When* (no reconciliation). *Else* may be null. *Fill* = the active branch expands to fill leftover height in a fixed-size (Resizable) window (so e.g. a meter's list scroll grows with the window). Default false — no effect in content-sized windows.

```csharp
public record ConditionalElement : IEquatable<HudElement>
```

## Public Members

| name | description |
| --- | --- |
| [ConditionalElement](ConditionalElement/ConditionalElement.md)(…) | React-style conditional. Both subtrees are built once; the renderer SetActive-toggles them each refresh from *When* (no reconciliation). *Else* may be null. *Fill* = the active branch expands to fill leftover height in a fixed-size (Resizable) window (so e.g. a meter's list scroll grows with the window). Default false — no effect in content-sized windows. |
| [Else](ConditionalElement/Else.md) { get; set; } |  |
| [Fill](ConditionalElement/Fill.md) { get; set; } |  |
| [Then](ConditionalElement/Then.md) { get; set; } |  |
| [When](ConditionalElement/When.md) { get; set; } |  |

## See Also

* record [HudElement](./HudElement.md)
* namespace [Stellar.Abstractions.Services](../Stellar.Abstractions.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stellar.Abstractions.dll -->
