# TileElement record

Icon tile (launcher): a centred PNG *Icon* over an optional *Label*, no background. Hover brightens icon+label and grows the icon ~1.18× (the native rail feel). The whole tile clicks via *OnClick*. When *Pinned* != null, a ★/☆ toggle overlays the top-right corner (its own click → *OnTogglePin*). *Label* null ⇒ icon-only (title-bar mode/rotate toggles); a non-null Func ALWAYS builds the label cell (so a live label that is empty at build time — e.g. a plugin that registers later — still shows its name once available).

```csharp
public record TileElement : IEquatable<HudElement>
```

## Public Members

| name | description |
| --- | --- |
| [TileElement](TileElement/TileElement.md)(…) | Icon tile (launcher): a centred PNG *Icon* over an optional *Label*, no background. Hover brightens icon+label and grows the icon ~1.18× (the native rail feel). The whole tile clicks via *OnClick*. When *Pinned* != null, a ★/☆ toggle overlays the top-right corner (its own click → *OnTogglePin*). *Label* null ⇒ icon-only (title-bar mode/rotate toggles); a non-null Func ALWAYS builds the label cell (so a live label that is empty at build time — e.g. a plugin that registers later — still shows its name once available). |
| [Icon](TileElement/Icon.md) { get; set; } |  |
| [IconSize](TileElement/IconSize.md) { get; set; } |  |
| [Label](TileElement/Label.md) { get; set; } |  |
| [OnClick](TileElement/OnClick.md) { get; set; } |  |
| [OnTogglePin](TileElement/OnTogglePin.md) { get; set; } |  |
| [Pinned](TileElement/Pinned.md) { get; set; } |  |
| [Width](TileElement/Width.md) { get; set; } |  |

## See Also

* record [HudElement](./HudElement.md)
* namespace [Stellar.Abstractions.Services](../Stellar.Abstractions.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stellar.Abstractions.dll -->
