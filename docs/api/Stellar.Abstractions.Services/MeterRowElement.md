# MeterRowElement record

One bespoke CombatMeter row — the borderless, role-coloured, animated meter row (HP spine + class crest + name·spec·share line + role-coloured metric bar with a per-second/total overlay + self highlight + offline scrim). The framework reproduces the custom visual that the IMGUI `MeterRowView` drew, so the meter keeps its distinct look (it does NOT ride the generic Row/Bar primitives). *Data* is re-pulled on the window's capped refresh (poll-diffed) — the plugin snapshots its per-combatant state so the Func never allocates. Fixed 48-px row height; width fills the row. Compose inside a [`ListElement`](./ListElement.md).

```csharp
public record MeterRowElement : IEquatable<HudElement>
```

## Public Members

| name | description |
| --- | --- |
| [MeterRowElement](MeterRowElement/MeterRowElement.md)(…) | One bespoke CombatMeter row — the borderless, role-coloured, animated meter row (HP spine + class crest + name·spec·share line + role-coloured metric bar with a per-second/total overlay + self highlight + offline scrim). The framework reproduces the custom visual that the IMGUI `MeterRowView` drew, so the meter keeps its distinct look (it does NOT ride the generic Row/Bar primitives). *Data* is re-pulled on the window's capped refresh (poll-diffed) — the plugin snapshots its per-combatant state so the Func never allocates. Fixed 48-px row height; width fills the row. Compose inside a [`ListElement`](./ListElement.md). |
| [Data](MeterRowElement/Data.md) { get; set; } | Per-combatant row snapshot, re-pulled on the window's capped refresh. |
| [OnRightClick](MeterRowElement/OnRightClick.md) { get; set; } | Optional: invoked when the row is right-clicked (CombatMeter row context menu); null = no menu. |

## See Also

* record [HudElement](./HudElement.md)
* namespace [Stellar.Abstractions.Services](../Stellar.Abstractions.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stellar.Abstractions.dll -->
