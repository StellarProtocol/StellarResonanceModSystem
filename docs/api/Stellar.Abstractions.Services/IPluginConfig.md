# IPluginConfig interface

Per-plugin persistent configuration. Each plugin gets one JSON file on disk (`<plugin-dir>/<pluginGuid>.config.json`) organized into named sections. Plugins read settings on construct, write whenever the user changes them, and subscribe to [`SectionChanged`](./IPluginConfig/SectionChanged.md) to react to external edits or settings-window writes from sibling code.

```csharp
public interface IPluginConfig
```

## Members

| name | description |
| --- | --- |
| event [SectionChanged](IPluginConfig/SectionChanged.md) | Fired when any section's underlying file changes — either via [`Save`](./IConfigSection/Save.md) from this process or an external edit detected by the file watcher. The string argument is the section name. Subscribers are called on the game thread. |
| [GetSection](IPluginConfig/GetSection.md)(…) | Returns the named section. Sections are created lazily on first access; the returned [`IConfigSection`](./IConfigSection.md) is stable across calls for the same name. |

## See Also

* namespace [Stellar.Abstractions.Services](../Stellar.Abstractions.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stellar.Abstractions.dll -->
