# SpacerElement record

Gap inside a Row. *Width* = 0 → flexible (expands to push following siblings to the far edge); &gt; 0 → a fixed-width spacer (e.g. to balance a right-aligned control so a centred element is TRULY centred, not centred-minus-that-control). *Height* &gt; 0 → a fixed-height gap (use in a Column for a little vertical margin between sections, no divider line).

```csharp
public record SpacerElement : IEquatable<HudElement>
```

## Public Members

| name | description |
| --- | --- |
| [SpacerElement](SpacerElement/SpacerElement.md)(…) | Gap inside a Row. *Width* = 0 → flexible (expands to push following siblings to the far edge); &gt; 0 → a fixed-width spacer (e.g. to balance a right-aligned control so a centred element is TRULY centred, not centred-minus-that-control). *Height* &gt; 0 → a fixed-height gap (use in a Column for a little vertical margin between sections, no divider line). |
| [Height](SpacerElement/Height.md) { get; set; } |  |
| [Width](SpacerElement/Width.md) { get; set; } |  |

## See Also

* record [HudElement](./HudElement.md)
* namespace [Stellar.Abstractions.Services](../Stellar.Abstractions.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stellar.Abstractions.dll -->
