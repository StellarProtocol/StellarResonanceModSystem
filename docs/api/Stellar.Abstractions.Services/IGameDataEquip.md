# IGameDataEquip interface

Static-data lookups for the gear-slot tables (equip rows, attr-lib ranges, slot names). Deferred-loaded: lookups return null/empty for a few seconds after boot.

```csharp
public interface IGameDataEquip
```

## Members

| name | description |
| --- | --- |
| [GetAttrLib](IGameDataEquip/GetAttrLib.md)(…) | Returns the attribute entries (with value ranges) for an attr-lib id, filtered to the row whose allow-part list contains *equipPart* (a lib id has one table row per slot-part group — an unfiltered merge mixes other slots' ranges in; ZDPS-verified semantics). Empty list when unknown / not yet loaded. |
| [GetAttrLibRow](IGameDataEquip/GetAttrLibRow.md)(…) | Returns the attribute entries for an attr-lib table ROW id, or an empty list. The wire's per-instance roll maps (`EquipAttr.basic_attr` etc.) key by row id with a 0–100 roll percentile value: displayed stat = `floor(pct * (Max - Min) / 100 + Min)` per entry (verified against the game's own `equip_attr_parse_vm.lua`, 2026-06-12). |
| [GetEnchantItem](IGameDataEquip/GetEnchantItem.md)(…) | Resolves a socketed gem from the wire's `(enchant_item_type_id, enchant_level)` pair to its [`EnchantItemInfo`](../Stellar.Abstractions.Domain.GameData/EnchantItemInfo.md) (gem item id whose name carries the display level + flat effects). Null if unknown / not yet loaded. |
| [GetEquipRow](IGameDataEquip/GetEquipRow.md)(…) | Returns the gear-piece row for *equipId*, or null if unknown / not yet loaded. |
| [GetSchoolAttrLib](IGameDataEquip/GetSchoolAttrLib.md)(…) | Returns the v2 SCHOOL attr-lib entries for an attr-lib id, filtered to the row whose allow-part list contains *equipPart* AND whose talent-school list contains *talentSchoolId* (resolve via `ProfessionSpecs.TalentSchool`). This is the spec-dependent advanced-roll source for raid/v2 gear. Empty when unknown / not yet loaded. |
| [GetSchoolAttrLibRow](IGameDataEquip/GetSchoolAttrLibRow.md)(…) | Like [`GetAttrLibRow`](./IGameDataEquip/GetAttrLibRow.md) but resolves against the v2 SCHOOL lib table — use for rolls flagged [`School`](../Stellar.Abstractions.Domain.Inventory/GearAttrRoll/School.md) (from `equip_attr_set`). Kept separate because a school row id can collide with a v1 row id. |
| [GetSlotName](IGameDataEquip/GetSlotName.md)(…) | Returns the localized slot label for an [`EquipPart`](../Stellar.Abstractions.Domain.GameData/EquipRowInfo/EquipPart.md) code, or null. |

## See Also

* namespace [Stellar.Abstractions.Services](../Stellar.Abstractions.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stellar.Abstractions.dll -->
