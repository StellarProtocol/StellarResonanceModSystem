# EquippedLoadout record

The LOCAL player's CURRENT LIVE equipped set, read from the game's live equip/mod containers (`CharSerialize.equip.equipList` / `CharSerialize.mod.modSlots`). Unlike the method-21 self-gear cache, this reflects manual equips, refines, and class-swap re-equips — so a consumer that snapshots per-class gear as each class is played captures what was ACTUALLY worn. Empty (never null lists) until the container resolves.

```csharp
public record EquippedLoadout
```

## Public Members

| name | description |
| --- | --- |
| [EquippedLoadout](EquippedLoadout/EquippedLoadout.md)(…) | The LOCAL player's CURRENT LIVE equipped set, read from the game's live equip/mod containers (`CharSerialize.equip.equipList` / `CharSerialize.mod.modSlots`). Unlike the method-21 self-gear cache, this reflects manual equips, refines, and class-swap re-equips — so a consumer that snapshots per-class gear as each class is played captures what was ACTUALLY worn. Empty (never null lists) until the container resolves. |
| [Gear](EquippedLoadout/Gear.md) { get; set; } | Equipped gear pieces with full rolls, refine level, and enchant; ordered by slot. |
| [Modules](EquippedLoadout/Modules.md) { get; set; } | Equipped modules with rolled parts, keyed by 1-based module slot. |

## See Also

* namespace [Stellar.Abstractions.Domain.Inventory](../Stellar.Abstractions.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stellar.Abstractions.dll -->
