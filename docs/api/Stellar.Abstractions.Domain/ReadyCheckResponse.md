# ReadyCheckResponse structure

One member's response to a dungeon ready-check, decoded from the `WorldNtf.NotifyCaptainReady` push (method 71). Raised by `IPartyEvents.ReadyCheckResponded` on the Unity main thread.

```csharp
public struct ReadyCheckResponse : IEquatable<ReadyCheckResponse>
```

| parameter | description |
| --- | --- |
| CharId | The responding member's character id (wire `v_char_id`). Matches [`CharId`](./PartyMember/CharId.md). |
| Name | The member's display name (wire `v_member_name`), or `null` if absent. |
| IsReady | `true` = ready, `false` = declined (wire `v_ready_info.is_ready`). |

## Public Members

| name | description |
| --- | --- |
| [ReadyCheckResponse](ReadyCheckResponse/ReadyCheckResponse.md)(…) | One member's response to a dungeon ready-check, decoded from the `WorldNtf.NotifyCaptainReady` push (method 71). Raised by `IPartyEvents.ReadyCheckResponded` on the Unity main thread. |
| [CharId](ReadyCheckResponse/CharId.md) { get; set; } | The responding member's character id (wire `v_char_id`). Matches [`CharId`](./PartyMember/CharId.md). |
| [IsReady](ReadyCheckResponse/IsReady.md) { get; set; } | `true` = ready, `false` = declined (wire `v_ready_info.is_ready`). |
| [Name](ReadyCheckResponse/Name.md) { get; set; } | The member's display name (wire `v_member_name`), or `null` if absent. |

## See Also

* namespace [Stellar.Abstractions.Domain](../Stellar.Abstractions.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stellar.Abstractions.dll -->
