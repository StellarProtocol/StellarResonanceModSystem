# EquipRowInfo structure

Static data for a gear piece from `Bokura.EquipTableBase` (the gear-slot table — distinct from [`ItemInfo`](./ItemInfo.md), which carries the same id's name/quality/icon).

```csharp
public struct EquipRowInfo : IEquatable<EquipRowInfo>
```

| parameter | description |
| --- | --- |
| Id | Equip/item id (shared with [`Id`](./ItemInfo/Id.md)). |
| EquipPart | Slot-part code (resolve to a label via `IGameDataEquip.GetSlotName`). |
| Gs | Base gear score of the piece. |
| WearLevel | Required wearing level (0 when the table row carries none). |
| PerfectCap | Maximum perfection value (0 when none). |
| BasicLibVersion | Lib-table selector for the basic libs: the FIRST element of the raw table column is a version marker, not a lib id (ZDPS-verified) — 1 = the normal EquipAttrLib table, 2 = the spec/school lib table (not loaded; spec-dependent values). |
| BasicAttrLibIds | Attr-lib ids for the fixed basic attributes (version marker already stripped; typically one lib per breakthrough tier — index 0 = breakthrough 0). |
| AdvancedLibVersion | Lib-table selector for the advanced libs (same semantics as *BasicLibVersion*). |
| AdvancedAttrLibIds | Attr-lib ids for the rolled advanced-attribute slots (version marker already stripped). Resolve each via `IGameDataEquip.GetAttrLib(libId, EquipPart)` — a lib id has one row per slot-part group, so the lookup is part-filtered. |

## Public Members

| name | description |
| --- | --- |
| [EquipRowInfo](EquipRowInfo/EquipRowInfo.md)(…) | Static data for a gear piece from `Bokura.EquipTableBase` (the gear-slot table — distinct from [`ItemInfo`](./ItemInfo.md), which carries the same id's name/quality/icon). |
| [AdvancedAttrLibIds](EquipRowInfo/AdvancedAttrLibIds.md) { get; set; } | Attr-lib ids for the rolled advanced-attribute slots (version marker already stripped). Resolve each via `IGameDataEquip.GetAttrLib(libId, EquipPart)` — a lib id has one row per slot-part group, so the lookup is part-filtered. |
| [AdvancedLibVersion](EquipRowInfo/AdvancedLibVersion.md) { get; set; } | Lib-table selector for the advanced libs (same semantics as *BasicLibVersion*). |
| [BasicAttrLibIds](EquipRowInfo/BasicAttrLibIds.md) { get; set; } | Attr-lib ids for the fixed basic attributes (version marker already stripped; typically one lib per breakthrough tier — index 0 = breakthrough 0). |
| [BasicLibVersion](EquipRowInfo/BasicLibVersion.md) { get; set; } | Lib-table selector for the basic libs: the FIRST element of the raw table column is a version marker, not a lib id (ZDPS-verified) — 1 = the normal EquipAttrLib table, 2 = the spec/school lib table (not loaded; spec-dependent values). |
| [EquipPart](EquipRowInfo/EquipPart.md) { get; set; } | Slot-part code (resolve to a label via `IGameDataEquip.GetSlotName`). |
| [Gs](EquipRowInfo/Gs.md) { get; set; } | Base gear score of the piece. |
| [Id](EquipRowInfo/Id.md) { get; set; } | Equip/item id (shared with [`Id`](./ItemInfo/Id.md)). |
| [PerfectCap](EquipRowInfo/PerfectCap.md) { get; set; } | Maximum perfection value (0 when none). |
| [WearLevel](EquipRowInfo/WearLevel.md) { get; set; } | Required wearing level (0 when the table row carries none). |

## See Also

* namespace [Stellar.Abstractions.Domain.GameData](../Stellar.Abstractions.md)

<!-- DO NOT EDIT: generated by xmldocmd for Stellar.Abstractions.dll -->
