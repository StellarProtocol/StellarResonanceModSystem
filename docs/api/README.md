# API reference — `Stellar.Abstractions`

This is the **generated reference for the plugin-facing API surface** — every public
interface, record, and enum a plugin can consume. Plugins reference **only**
`Stellar.Abstractions`, so this is the complete contract you build against.

Start at [**`Stellar.Abstractions.md`**](Stellar.Abstractions.md) for the full type index, or
jump to the aggregator [`IPluginServices`](Stellar.Abstractions.Services/IPluginServices.md).

For a narrative walkthrough (lifecycle, the uGUI toolkit, patterns), see
[`../plugin-development.md`](../plugin-development.md).

> Generated from XML docs with [xmldocmd](https://github.com/ejball/XmlDocMarkdown). Regenerate
> after changing the public surface; do not hand-edit.
