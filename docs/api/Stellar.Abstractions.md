# Stellar.Abstractions assembly

## Stellar.Abstractions.Diagnostics namespace

| public type | description |
| --- | --- |
| static class [PerfControls](./Stellar.Abstractions.Diagnostics/PerfControls.md) | Runtime bisect toggles for the overlay perf harness, mutated from the perf overlay and read by `UiBuilderService.Tick` and `ThemeRenderer`. Only meaningful while [`IsEnabled`](./Stellar.Abstractions.Diagnostics/PerfProbe/IsEnabled.md) — callers still read these unconditionally, but the overlay that flips them only exists when the harness is enabled, so production state stays at the inert defaults. |
| static class [PerfProbe](./Stellar.Abstractions.Diagnostics/PerfProbe.md) | Per-frame timing accumulator for the overlay perf harness. Gated by `STELLAR_PERFHUD=1` (mirrors [`StellarDiagnostics`](./Stellar.Abstractions.Diagnostics/StellarDiagnostics.md)): when disabled every method early-returns on a single cached field read so production pays nothing. |
| class [PerfSnapshot](./Stellar.Abstractions.Diagnostics/PerfSnapshot.md) | Snapshot of one committed frame's timings. |
| static class [StellarDiagnostics](./Stellar.Abstractions.Diagnostics/StellarDiagnostics.md) | Diagnostic mode toggle. Set environment variable `STELLAR_DIAGNOSTICS=1` before launching the game (in Heroic's env-var settings) to enable per-event logs in `PandaChatProbe`, `PandaWireTap`, `PandaCombatStubProbe`, and `ChatService`. Default off so normal play produces minimal log volume. |

## Stellar.Abstractions.Domain namespace

| public type | description |
| --- | --- |
| struct [ActiveBuff](./Stellar.Abstractions.Domain/ActiveBuff.md) | One row of `BuffInfoSync.BuffInfos`. Field names map to the `BuffInfo` proto. |
| enum [BuffChangeKind](./Stellar.Abstractions.Domain/BuffChangeKind.md) | How a buff's state changed, as reported by [`BuffChanged`](./Stellar.Abstractions.Domain/CombatEvent.BuffChanged.md). |
| enum [ChatChannel](./Stellar.Abstractions.Domain/ChatChannel.md) | Incoming-chat channel bucket. Mirrors a subset of `Zproto.ChitChatChannelType`. |
| record [ChatMessage](./Stellar.Abstractions.Domain/ChatMessage.md) | An incoming chat message delivered to plugins. `MsgId` is the server-assigned globally-unique id; plugins can use it to deduplicate when the same message arrives via multiple paths (live Notify + history Return). `IsHistory` is true for messages projected from the `GetChipChatRecords` reply that the server sends at login — plugins that react to live traffic (e.g. auto-reply, sound notifications) should skip history-replay messages by checking this flag. |
| enum [ChatMessageType](./Stellar.Abstractions.Domain/ChatMessageType.md) | Incoming-message classification. Mirrors `Zproto.ChitChatMsgType`. |
| abstract record [ChatTarget](./Stellar.Abstractions.Domain/ChatTarget.md) | Sealed sum type identifying the destination for `IChat.Send`. |
| struct [ColorRgba](./Stellar.Abstractions.Domain/ColorRgba.md) | Plain RGBA float colour. Mirrors UnityEngine.Color shape without depending on Unity. |
| abstract record [CombatEvent](./Stellar.Abstractions.Domain/CombatEvent.md) | Discriminated event raised by [`ICombatEvents`](./Stellar.Abstractions.Services/ICombatEvents.md). Always fires on the main (Unity) thread. |
| enum [DamageElement](./Stellar.Abstractions.Domain/DamageElement.md) | Damage-element enum, mirroring `EDamageProperty` in the BPSR proto schema (see `e_damage_property.py` in the BPSR-Meter reference). Surfaces on `CombatEvent.DamageDealt.Element` via `SyncDamageInfo.Property` (field 18). |
| enum [DamageSourceKind](./Stellar.Abstractions.Domain/DamageSourceKind.md) | Damage-source enum mirroring `EDamageSource` on the wire (see `e_damage_source.py` in the BPSR-Meter reference). Values must match the wire int because `SyncDamageInfo.DamageSource` is cast straight into this enum. Note `Other` is 100 (not 5) per the canonical proto — keep this in sync with the live game. |
| struct [EntityId](./Stellar.Abstractions.Domain/EntityId.md) | Strongly-typed wrapper around the int64 entity UUID used by the BPSR world server. The low 16 bits encode the entity type (640 = player, 64 / 32832 = monster); the upper bits encode the numeric uid. |
| struct [EntityVitals](./Stellar.Abstractions.Domain/EntityVitals.md) | Live HP snapshot for an entity, sourced from `AttrCollection` observations on the combat wire (`AttrHp`=11310, `AttrMaxHp`=11320). Available for every entity in AOI — players, mobs, NPCs — not just party members. Use [`GetVitals`](./Stellar.Abstractions.Services/ICombatLookup/GetVitals.md) to query. |
| struct [EquipNineEntry](./Stellar.Abstractions.Domain/EquipNineEntry.md) | One equipped slot from the wire's `AttrEquipData` (`EquipNine`): which item id occupies which equipment slot. Names/stats are resolved separately from game-data tables. |
| struct [FashionEntry](./Stellar.Abstractions.Domain/FashionEntry.md) | One worn cosmetic from the broadcast `AttrFashionData` attribute (id 201) — available for ANY player in AOI (it is how the game renders other players' outfits). |
| static class [FrameworkVersion](./Stellar.Abstractions.Domain/FrameworkVersion.md) | Single source of truth for the framework's version string. Lives in Abstractions so every layer (Host's BepInPlugin manifest, Infrastructure's AboutPanel, and any plugin that wants to gate behaviour on a particular framework release) can read the same constant without re-declaring it. Bump this on each user-visible release; Host's `BootstrapPlugin.PluginVersion` forwards to it so the BepInEx plugin manifest stays in lockstep. |
| record [HotkeyAction](./Stellar.Abstractions.Domain/HotkeyAction.md) | Descriptor for a bindable keyboard action declared via [`DeclareAction`](./Stellar.Abstractions.Services/IHotkeys/DeclareAction.md). |
| interface [IHotkeyAction](./Stellar.Abstractions.Domain/IHotkeyAction.md) | Handle returned by [`DeclareAction`](./Stellar.Abstractions.Services/IHotkeys/DeclareAction.md). Dispose to unregister the action. |
| enum [ImaginePosition](./Stellar.Abstractions.Domain/ImaginePosition.md) | Where the Battle-Imagine cluster sits in a meter row. |
| enum [ImagineSize](./Stellar.Abstractions.Domain/ImagineSize.md) | Battle-Imagine icon size in a meter row. |
| struct [ImagineSlot](./Stellar.Abstractions.Domain/ImagineSlot.md) | One equipped Battle Imagine's render state for a meter row's trailing icons. |
| struct [KeyBinding](./Stellar.Abstractions.Domain/KeyBinding.md) | A keyboard shortcut: a primary key plus zero or more modifier keys. |
| enum [MenuButtonStyle](./Stellar.Abstractions.Domain/MenuButtonStyle.md) | Visual style for chrome buttons (GlassMenu panels). See [`IChromeStyle`](./Stellar.Abstractions.Services/IChromeStyle.md). |
| enum [MenuScrollbarStyle](./Stellar.Abstractions.Domain/MenuScrollbarStyle.md) | Visual style for the vertical scrollbar (GlassMenu panels). |
| [Flags] enum [ModifierKeys](./Stellar.Abstractions.Domain/ModifierKeys.md) | Bit-flag set of keyboard modifier keys used in [`KeyBinding`](./Stellar.Abstractions.Domain/KeyBinding.md). |
| enum [NativeUiAnchor](./Stellar.Abstractions.Domain/NativeUiAnchor.md) | Curated game-UI regions a mod element may be injected into. |
| enum [PartyLeaveKind](./Stellar.Abstractions.Domain/PartyLeaveKind.md) | Why a party member left. Exact `leave_type` int-to-enum mapping is verified at bring-up; enum stays loose to absorb new wire codes via Unknown. |
| struct [PartyMember](./Stellar.Abstractions.Domain/PartyMember.md) | One party member's snapshot. Composed from wire deliveries on the `GrpcTeamNtf` service. Immutable; rebuilt by `PartyService` whenever any field changes. |
| enum [PartyType](./Stellar.Abstractions.Domain/PartyType.md) | Party size category. Matches `Zproto.ETeamMemberType`. |
| record [PluginInfo](./Stellar.Abstractions.Domain/PluginInfo.md) | Snapshot of a plugin slot known to `IPluginInventory`. The Settings → Plugins panel renders one row per [`PluginInfo`](./Stellar.Abstractions.Domain/PluginInfo.md). |
| struct [Position3D](./Stellar.Abstractions.Domain/Position3D.md) | World-space position of a game entity. Plain C# struct — intentionally has no Unity dependency so [`IPlayerState`](./Stellar.Abstractions.Services/IPlayerState.md) stays in the BCL-only contracts layer. |
| struct [Resolution](./Stellar.Abstractions.Domain/Resolution.md) | Screen or texture resolution in integer pixels. |
| struct [SenderMeta](./Stellar.Abstractions.Domain/SenderMeta.md) | Optional sender metadata; null on [`ChatMessage`](./Stellar.Abstractions.Domain/ChatMessage.md) when the probe could not resolve. |
| struct [SkillCooldown](./Stellar.Abstractions.Domain/SkillCooldown.md) | One row of `AoiSyncToMeDelta.SyncSkillCDs`. Times are server epoch ms. |
| enum [SkillCooldownKind](./Stellar.Abstractions.Domain/SkillCooldownKind.md) | Matches the wire enum `ESkillCDType`. |
| enum [SkillEventPhase](./Stellar.Abstractions.Domain/SkillEventPhase.md) | Subset of `ESkillEventType` exposed on [`SkillUsed`](./Stellar.Abstractions.Domain/CombatEvent.SkillUsed.md). |
| struct [SkillLevel](./Stellar.Abstractions.Domain/SkillLevel.md) | One entry of an entity's equipped skill loadout, decoded from the combat wire's `AttrSkillLevelIdList` attribute (`EAttrType`=116). The server broadcasts this per-entity for every player in AOI, so it is the canonical source for a player's equipped skills — including Battle Imagines (skills whose `SlotPositionId` contains 7 or 8). |
| enum [StellarKeyCode](./Stellar.Abstractions.Domain/StellarKeyCode.md) | Subset of UnityEngine.KeyCode values. Integer values match UnityEngine.KeyCode exactly so Infrastructure can cast in either direction. Abstractions cannot reference UnityEngine, so this mirror lives in plain BCL. |
| enum [ThemePreset](./Stellar.Abstractions.Domain/ThemePreset.md) | Named palette family applied to every plugin window. |
| struct [UvRect](./Stellar.Abstractions.Domain/UvRect.md) | Normalized atlas sub-rect (0..1, origin bottom-left, Unity texture-space convention) for [`SpriteElement`](./Stellar.Abstractions.Services/SpriteElement.md). A plain BCL struct so the Abstractions layer carries no Unity dependency — the renderer maps it to `UnityEngine.Rect`. |
| enum [WindowCategory](./Stellar.Abstractions.Domain/WindowCategory.md) | Logical category for a plugin window, used to group windows in the Settings layout editor. |
| enum [WindowPanelStyle](./Stellar.Abstractions.Domain/WindowPanelStyle.md) | Visual chrome style for a registered plugin window. Passed via [`Style`](./Stellar.Abstractions.Domain/WindowSpec/Style.md). |
| struct [WindowRect](./Stellar.Abstractions.Domain/WindowRect.md) | Plain (X, Y, Width, Height) rectangle used for window positions and sizes. Named `WindowRect` to avoid collision with `UnityEngine.Rect` in plugin/UI consumers. |
| record [WindowSpec](./Stellar.Abstractions.Domain/WindowSpec.md) | Immutable declaration of a plugin window's identity, initial geometry, and chrome options. Passed to [`Register`](./Stellar.Abstractions.Services/IWindowHost/Register.md) to create the window. |

## Stellar.Abstractions.Domain.GameData namespace

| public type | description |
| --- | --- |
| struct [AchievementInfo](./Stellar.Abstractions.Domain.GameData/AchievementInfo.md) | Static data for a single achievement entry from the game table. |
| struct [ActivityInfo](./Stellar.Abstractions.Domain.GameData/ActivityInfo.md) | Static data for a timed in-game activity from the game table. |
| enum [AttributeGroup](./Stellar.Abstractions.Domain.GameData/AttributeGroup.md) | Attribute screen group. `Unknown = 0`; the first five named members mirror the in-game profile grouping (Offensive … ElementalBonus); the remaining members are catalog groups (Core … Deprecated) assigned by the generated attribute catalog. |
| struct [AttributeInfo](./Stellar.Abstractions.Domain.GameData/AttributeInfo.md) | Static data for a single character attribute from the game table. |
| struct [AttributeProfileInfo](./Stellar.Abstractions.Domain.GameData/AttributeProfileInfo.md) | UI panel classification for an attribute, sourced from `Bokura.ProfileAttrTableBase`. [`Type`](./Stellar.Abstractions.Domain.GameData/AttributeProfileInfo/Type.md) is the raw classifier int (1=Offensive, 2=Defensive, 3=Support, 4=ElementalAttack, 5=ElementalBonus per observed game data — verify in iteration 1); [`TypeDisplayName`](./Stellar.Abstractions.Domain.GameData/AttributeProfileInfo/TypeDisplayName.md) is the localized group label. |
| struct [AwardInfo](./Stellar.Abstractions.Domain.GameData/AwardInfo.md) | Static data for an in-game reward/award item from the game table. |
| enum [BuffCategory](./Stellar.Abstractions.Domain.GameData/BuffCategory.md) | Buff classification. Projected from the buff row's category int field. |
| struct [BuffInfo](./Stellar.Abstractions.Domain.GameData/BuffInfo.md) | Static data for one buff/debuff row (BuffTable). |
| struct [DamageAttrInfo](./Stellar.Abstractions.Domain.GameData/DamageAttrInfo.md) | Static data for a damage-attribute definition from the game table. |
| struct [DungeonInfo](./Stellar.Abstractions.Domain.GameData/DungeonInfo.md) | Static data for a dungeon / instanced content entry from the game table. |
| struct [EnchantEffect](./Stellar.Abstractions.Domain.GameData/EnchantEffect.md) | One flat stat grant on a gem: an attribute id and its (non-rolled) value. |
| struct [EnchantItemInfo](./Stellar.Abstractions.Domain.GameData/EnchantItemInfo.md) | One `EquipEnchantItemTable` row — a socketed gem at a specific level. Resolved from the wire's `(enchant_item_type_id, enchant_level)` pair: the wire level is an internal progression index, NOT the displayed level — the displayed level is baked into the gem ITEM's name ([`GemItemId`](./Stellar.Abstractions.Domain.GameData/EnchantItemInfo/GemItemId.md) → item-table name, e.g. "Crimson Foxen Sigil Lv.2"). |
| struct [EquipAttrRange](./Stellar.Abstractions.Domain.GameData/EquipAttrRange.md) | One attribute entry of an equip attr-lib row (`Bokura.EquipAttrLibTableBase`): the attribute id plus its rolled value range. For BASIC libs min equals max (a fixed value); for ADVANCED libs the range is the roll space shown to inspecting players. |
| struct [EquipInfo](./Stellar.Abstractions.Domain.GameData/EquipInfo.md) | Static data for a single equipment piece from the game table. |
| struct [EquipRowInfo](./Stellar.Abstractions.Domain.GameData/EquipRowInfo.md) | Static data for a gear piece from `Bokura.EquipTableBase` (the gear-slot table — distinct from [`ItemInfo`](./Stellar.Abstractions.Domain.GameData/ItemInfo.md), which carries the same id's name/quality/icon). |
| class [ImagineCooldownCalc](./Stellar.Abstractions.Domain.GameData/ImagineCooldownCalc.md) | Shared cooldown math for Battle-Imagine (and any multi-charge) skills, used by both the CooldownBar and the CombatMeter so their timing can't drift. Two parts: |
| struct [ImagineInfo](./Stellar.Abstractions.Domain.GameData/ImagineInfo.md) | Static data for one Battle Imagine (Resonance Skill): display + cooldown/charge model. |
| struct [ItemInfo](./Stellar.Abstractions.Domain.GameData/ItemInfo.md) | Static data for a single item from the game table. |
| enum [ItemKind](./Stellar.Abstractions.Domain.GameData/ItemKind.md) | Item category. Projected from `ItemTableBase.Type` (int). |
| struct [MapInfo](./Stellar.Abstractions.Domain.GameData/MapInfo.md) | Static data for a single map / zone from the game table. |
| struct [MonsterInfo](./Stellar.Abstractions.Domain.GameData/MonsterInfo.md) | Static data for a single monster type from the game table. |
| struct [NpcInfo](./Stellar.Abstractions.Domain.GameData/NpcInfo.md) | Static data for a single NPC from the game table. |
| struct [ProfessionInfo](./Stellar.Abstractions.Domain.GameData/ProfessionInfo.md) | Static data for a player profession / class from the game table. |
| static class [ProfessionSpecs](./Stellar.Abstractions.Domain.GameData/ProfessionSpecs.md) | Static skill-id → sub-profession (spec) knowledge. There is no wire field for spec — it is derived from the skills a player has equipped (the AOI `AttrSkillLevelIdList` loadout carries each spec's signature skills, so the spec is known the moment a player appears, before any combat) or, as a fallback, from the skills they cast. Mapping ported from the BPSR-ZDPS reference (`Professions.GetSubProfessionIdBySkillId`). |
| struct [QuestInfo](./Stellar.Abstractions.Domain.GameData/QuestInfo.md) | Static data for a single quest from the game table. |
| struct [SceneInfo](./Stellar.Abstractions.Domain.GameData/SceneInfo.md) | Static data for a single Unity scene entry from the game table. |
| struct [SkillInfo](./Stellar.Abstractions.Domain.GameData/SkillInfo.md) | Static data for a single skill from the game table. |
| enum [SkillKind](./Stellar.Abstractions.Domain.GameData/SkillKind.md) | Skill classification. Projected from `SkillTableBase.SkillType` (int). |
| struct [TalentInfo](./Stellar.Abstractions.Domain.GameData/TalentInfo.md) | Static data for a single talent / passive ability from the game table. |
| struct [TitleInfo](./Stellar.Abstractions.Domain.GameData/TitleInfo.md) | Static data for a player title from the game table. |
| struct [WeaponInfo](./Stellar.Abstractions.Domain.GameData/WeaponInfo.md) | Static data for a weapon type from the game table. |
| enum [WeaponKind](./Stellar.Abstractions.Domain.GameData/WeaponKind.md) | Weapon classification. Projected from the weapon row's weapon-class int. |

## Stellar.Abstractions.Domain.Inventory namespace

| public type | description |
| --- | --- |
| record [EquippedSet](./Stellar.Abstractions.Domain.Inventory/EquippedSet.md) | Currently equipped module UUIDs keyed by 1-based slot index. The game uses 1..`ModSlotMaxCount` = 1..4 (see `lua/ui/model/mod_define.lua`). Empty slots are ABSENT from the dictionary — callers should check `ModuleUuidsBySlot.ContainsKey(slot)` rather than expect a nullable value. |
| enum [EquipResult](./Stellar.Abstractions.Domain.Inventory/EquipResult.md) | Outcome of an [`IModuleEquip`](./Stellar.Abstractions.Services/IModuleEquip.md) dispatch. Maps the game's `EErrorCode` reply to plugin-friendly cases — plugins should never need to read raw error codes. |
| struct [GearAttrRoll](./Stellar.Abstractions.Domain.Inventory/GearAttrRoll.md) | One ROLLED attribute slot on an owned gear piece. The wire stores the roll as an `EquipAttrLibTable` ROW id plus a 0–100 roll PERCENTILE — not an attribute id/value pair. Resolve via `IGameDataEquip.GetAttrLibRow(LibRowId)`: each returned entry's displayed stat is `floor(Percentile * (Max - Min) / 100 + Min)` (the game's own formula in `equip_attr_parse_vm.lua`; verified against the live gear sheet 2026-06-12). |
| record [GearAttrRolls](./Stellar.Abstractions.Domain.Inventory/GearAttrRolls.md) | The four rolled-attribute groups of an owned gear piece (wire `EquipAttr` maps `basic_attr`, `advance_attr`, `recast_attr`, `rare_quality_attr`). Lists are never null; empty groups use shared empty arrays. |
| struct [GearEnchant](./Stellar.Abstractions.Domain.Inventory/GearEnchant.md) | Socketed enchant on an owned gear piece (wire `EquipEnchantInfo`, keyed by item uuid in `EquipList.equip_enchant`). |
| record [GearInstance](./Stellar.Abstractions.Domain.Inventory/GearInstance.md) | One equipped gear piece of the LOCAL player, decoded from the container sync — per-piece ACTUAL rolls (other players only expose table ranges via the equip tables). |
| struct [GearPerfection](./Stellar.Abstractions.Domain.Inventory/GearPerfection.md) | Perfection state of an owned gear piece (wire `EquipAttr` fields `perfection_value`/`max_perfection_value`/`perfection_level`). |
| enum [ModuleCategory](./Stellar.Abstractions.Domain.Inventory/ModuleCategory.md) | Module category as defined by the game's `ModType` enum (see `lua/ui/model/mod_define.lua: ModType`). The game caps the number of equipped modules per category — exceeding the cap triggers a SlotConflict on equip. |
| record [ModuleInfo](./Stellar.Abstractions.Domain.Inventory/ModuleInfo.md) | A single module item from the player's inventory. UUID is the unique instance key the game's equip RPC consumes; ConfigId is the table-row key for `ModTableMgr` (look up via a future `IGameDataInventory.GetMod`). |
| record [ModulePart](./Stellar.Abstractions.Domain.Inventory/ModulePart.md) | A single attribute contribution from a module. A module typically has 1–3 parts (see `lua/ui/model/mod_define.lua: ModEffectMaxCount = 3`). |
| record [ModuleSnapshot](./Stellar.Abstractions.Domain.Inventory/ModuleSnapshot.md) | Immutable snapshot of all module-package items currently held by the player. Produced by `IInventoryProbe.TryReadModules` at the 1Hz refresh tick and surfaced via [`GetModules`](./Stellar.Abstractions.Services/IInventory/GetModules.md). |

## Stellar.Abstractions.Plugins namespace

| public type | description |
| --- | --- |
| interface [IStellarPlugin](./Stellar.Abstractions.Plugins/IStellarPlugin.md) | Implemented by every Stellar plugin. The plugin host instantiates the type via its single constructor that takes [`IPluginServices`](./Stellar.Abstractions.Services/IPluginServices.md). |

## Stellar.Abstractions.Services namespace

| public type | description |
| --- | --- |
| record [AccentRowElement](./Stellar.Abstractions.Services/AccentRowElement.md) | Wraps a row *Child* with a per-row accent backdrop drawn BEHIND it: a faint *Stripe*-tinted bar whose width is *Share* (0..1) of the row, plus a 3-px *Stripe*-coloured left edge. The CombatMeter History/Skill table rows use this for the role-coloured stripe + share-fraction wash the IMGUI build drew (DrawRowAccent). Both Funcs are poll-diffed. |
| record [BarElement](./Stellar.Abstractions.Services/BarElement.md) | Graphical fill bar (0..1). Chrome framework-themed; *Fill* is the plugin's semantic colour (from its colour slot). Optional right-aligned numeric *Label* and optional fixed-width left *Prefix* caption (e.g. "HP" / "Stamina") so stacked bars align in a column. |
| record [BarWidget](./Stellar.Abstractions.Services/BarWidget.md) | Horizontal fill bar child widget for a [`PanelSpec`](./Stellar.Abstractions.Services/PanelSpec.md). |
| record [BrandLogoElement](./Stellar.Abstractions.Services/BrandLogoElement.md) | Animated brand logo (the launcher's Stellar sparkle): an accent-tinted *Sparkle* inside an accent button cell, over a soft *Glow* halo whose alpha + scale PULSE. The renderer drives the pulse each frame (the builder is sandbox-pure → renders static in the sandbox). *Size* is the sparkle size (default 22). |
| record [ButtonElement](./Stellar.Abstractions.Services/ButtonElement.md) | Clickable button. *Enabled* null = always enabled. *Style* null = the chrome's active IChromeStyle default. *Active* (poll-diffed) renders the accent/filled look when true — used for active tab / toggle-button highlighting. *Width* &gt; 0 fixes the button width (it can't grow/shrink) and WRAPS its label — use for right-aligned cells (e.g. a hotkey binding) that must never overflow the row regardless of font metrics. *Icon* (optional PNG) renders a leading icon INSIDE the button, co-centred with the label by the button's own layout (so icon↔label alignment can't drift with font metrics — used by the Settings hub's iconed tabs). |
| record [CellElement](./Stellar.Abstractions.Services/CellElement.md) | Width-controlled column cell — fixes or weights a cell's width for aligning a Row's children into a table. Wraps any *Child* subtree. *Width* &gt; 0 fixes the cell width (cannot grow/shrink — use for numeric columns like Current / Proj / Δ so they align across the header + every body row regardless of font metrics). *Weight* &gt; 0 makes the cell GROW to share leftover row width in that proportion (the elastic label column, or master-detail panes — Weight 1 : 2 = ⅓ : ⅔). Width and Weight are mutually exclusive (Width wins if both set); both 0 = natural content size. Per-cell colour stays on the Child (e.g. [`Color`](./Stellar.Abstractions.Services/TextElement/Color.md)) — the cell owns geometry only. Header and body align by using the SAME cell specs on every Row. Weighted cells require the enclosing Row to be a direct child of a Column / Scroll content / window body (all force-expand width — true for every layout that needs alignment). |
| record [ColorPickerElement](./Stellar.Abstractions.Services/ColorPickerElement.md) | HSV colour picker (SV square + hue bar + hex field). *Get* reflects the slot's colour; a pick calls *Set*. The one hand-drawn custom widget. |
| record [ColorSlotInfo](./Stellar.Abstractions.Services/ColorSlotInfo.md) | Editor enumeration record for one registered slot. |
| record [ColumnElement](./Stellar.Abstractions.Services/ColumnElement.md) | Vertical layout container: children stacked top-to-bottom with optional spacing. |
| record [ConditionalElement](./Stellar.Abstractions.Services/ConditionalElement.md) | React-style conditional. Both subtrees are built once; the renderer SetActive-toggles them each refresh from *When* (no reconciliation). *Else* may be null. *Fill* = the active branch expands to fill leftover height in a fixed-size (Resizable) window (so e.g. a meter's list scroll grows with the window). Default false — no effect in content-sized windows. |
| record [CooldownTileElement](./Stellar.Abstractions.Services/CooldownTileElement.md) | One cooldown/debuff tile for the CooldownBar: a fixed icon square (game-asset art via *Icon*/*Uv*) with an *Accent*-tinted outline, a foot fill-bar (width = *Fill01*, 0..1), an optional ★ corner badge when *IsImagine*, an optional ×N charge count when *ChargeCount* &gt; 1, and a centred *Seconds* caption below — all poll-diffed each refresh. The framework binds the boxed texture on the window's refresh pass (renders without the runtime ticker, unlike [`GameTextureElement`](./Stellar.Abstractions.Services/GameTextureElement.md)), so it appears immediately. Compose pooled tiles inside a [`RowElement`](./Stellar.Abstractions.Services/RowElement.md) wrapped in [`ConditionalElement`](./Stellar.Abstractions.Services/ConditionalElement.md)s for active-only collapse. |
| record [DragSlotElement](./Stellar.Abstractions.Services/DragSlotElement.md) | Wraps a grid cell so it can be dragged onto another cell. *Key* identifies the cell (the CombatMeter uses the flat `group*5+slot` index). *OnDrop* fires on the grabbed cell when it is released over a different registered cell, as `OnDrop(fromKey, toKey)`. *CanDrag* gates the whole interaction (e.g. leader-only); when it returns `false` the cell neither drags nor accepts drops. While dragging, a dimmed ghost of *Child* follows the cursor and the hovered target highlights. Renderer-neutral — no IMGUI tokens. |
| struct [EntityMenuItem](./Stellar.Abstractions.Services/EntityMenuItem.md) | A registered context-menu item, scoped to the entity it was resolved for. |
| struct [EquippedItem](./Stellar.Abstractions.Services/EquippedItem.md) | One equipped item resolved for display: which item id occupies which slot. |
| record [GameTextureElement](./Stellar.Abstractions.Services/GameTextureElement.md) | Displays a game-asset texture handle (boxed `UnityEngine.Texture`) at a fixed pixel size, optionally cropped to a UV sub-rect (for atlas icons such as profession crests). The framework re-pulls *Texture* (and *Uv* when set) each frame, so an icon whose async load finishes after the window builds still appears. Renderer-neutral — the handle crosses the boundary as Object. Null texture = an invisible box that keeps its layout slot. The simpler sibling of [`RenderTextureHostElement`](./Stellar.Abstractions.Services/RenderTextureHostElement.md) (no drag/zoom/pan, no backdrop) and of [`SpriteElement`](./Stellar.Abstractions.Services/SpriteElement.md) (which takes PNG bytes, not a live texture handle). |
| struct [GearSlotRef](./Stellar.Abstractions.Services/GearSlotRef.md) | One equipped item from social data: which item-config id occupies which slot (no instance rolls — social data carries slot+id only). |
| enum [HudAnchor](./Stellar.Abstractions.Services/HudAnchor.md) | HUD placement. FreeOverlay = the draggable, position-persisted Stellar HUD layer. |
| abstract record [HudElement](./Stellar.Abstractions.Services/HudElement.md) | A node in a HUD's declarative element tree. Plugins compose these; the framework renders them as native uGUI with one enforced chrome. Dynamic leaves carry `Func`s the framework re-pulls on its capped refresh (state→view without touching the UI engine). |
| record [HudSpec](./Stellar.Abstractions.Services/HudSpec.md) | A plugin's HUD: stable id, anchor, and the root element to render. *AutoHideBehindGameMenus* (default true) hides the HUD while a full-screen game menu is open — gameplay HUDs (cooldowns, meter, stats) should not draw over the inventory/menu. *HideUntilInWorld* keeps it hidden until the player is logged in. *DefaultRect* is the spawn position used on first run / when no layout is saved for the current resolution (clamped on-screen by the layout layer); null falls back to the renderer's initial placement (top-left). |
| interface [IChat](./Stellar.Abstractions.Services/IChat.md) | Plugin-facing chat service. `MessageReceived` fires on the main (Unity) thread; `Send` is fire-and-forget — failures are logged, never thrown. |
| interface [IChromeStyle](./Stellar.Abstractions.Services/IChromeStyle.md) | User-selected global chrome control styles — the button + scrollbar look applied to every GlassMenu panel. Persisted with the theme; changing either fires the theme's [`ActiveChanged`](./Stellar.Abstractions.Services/INamedTheme/ActiveChanged.md) so the renderer rebuilds its skin. A plugin may pin its own per-window button and scrollbar style via [`ButtonElement`](./Stellar.Abstractions.Services/ButtonElement.md), which the user cannot override here. |
| interface [IClientState](./Stellar.Abstractions.Services/IClientState.md) | Player session state. Mirrors lifecycle events the game itself publishes. |
| interface [IColorRegistry](./Stellar.Abstractions.Services/IColorRegistry.md) | Plugins register the colours they OWN. Key is namespaced "Owner.Concept.Property" (e.g. "PlayerHUD.StaminaBar.Fill"), unique. A `Register` overload supplies the owner's colour for each built-in preset. Register only colours a plugin owns; to match a theme colour, read `Theme.Colors.X` directly instead. |
| interface [IColorSlot](./Stellar.Abstractions.Services/IColorSlot.md) | Handle returned by [`IColorRegistry`](./Stellar.Abstractions.Services/IColorRegistry.md)'s `Register` methods. Read [`Value`](./Stellar.Abstractions.Services/IColorSlot/Value.md) when drawing — it resolves to the colour for the active theme, honouring user overrides. Cache the handle, not the value. |
| interface [ICombatEvents](./Stellar.Abstractions.Services/ICombatEvents.md) | Combat event stream. Mirrors the event half of the original mixed combat surface; polled state lives on [`ICombatSnapshot`](./Stellar.Abstractions.Services/ICombatSnapshot.md) and per-entity lookups on [`ICombatLookup`](./Stellar.Abstractions.Services/ICombatLookup.md). Events always fire on the Unity main thread (drained once per `Game.Update` postfix). |
| interface [ICombatLookup](./Stellar.Abstractions.Services/ICombatLookup.md) | Per-entity combat lookups. Covers any entity observed in AOI on the combat wire — not limited to party members or the local player. Mirrors the lookup half of the original mixed combat surface; the polled local snapshot lives on [`ICombatSnapshot`](./Stellar.Abstractions.Services/ICombatSnapshot.md) and the event stream lives on [`ICombatEvents`](./Stellar.Abstractions.Services/ICombatEvents.md). |
| interface [ICombatSnapshot](./Stellar.Abstractions.Services/ICombatSnapshot.md) | Polled snapshot of the local player's combat state plus a ring buffer of recently observed events. Mirrors the polled half of the original mixed combat surface; the event stream lives on [`ICombatEvents`](./Stellar.Abstractions.Services/ICombatEvents.md) and per-entity lookups live on [`ICombatLookup`](./Stellar.Abstractions.Services/ICombatLookup.md). All reads are safe from the Unity main thread. |
| interface [IConfigSection](./Stellar.Abstractions.Services/IConfigSection.md) | A named subset of a plugin's configuration. Read access via [`Get`](./Stellar.Abstractions.Services/IConfigSection/Get.md) is lock-free against the in-memory cache; writes via [`Set`](./Stellar.Abstractions.Services/IConfigSection/Set.md) are cached and persisted to disk on [`Save`](./Stellar.Abstractions.Services/IConfigSection/Save.md). Supported `T`: primitives (Int32, Int64, Boolean, String, Single, Double), arrays of primitives, and string-keyed dictionaries of primitives. Records and complex objects are out of scope for v1 — use multiple keys instead. |
| interface [IEntityContextMenu](./Stellar.Abstractions.Services/IEntityContextMenu.md) | Extension point for entity-scoped row context menus (CombatMeter renders these on a right-click). Any plugin registers items; the renderer need not know the registrant. Items can gate per-entity. |
| interface [IEntityDetail](./Stellar.Abstractions.Services/IEntityDetail.md) | Per-entity detail captured from the combat wire's `SyncNearEntities` attribute stream — the full broadcast attribute map and equipment for any in-AOI entity. Identity/skills come from [`ICombatLookup`](./Stellar.Abstractions.Services/ICombatLookup.md); derived self-only stats from [`IPlayerStats`](./Stellar.Abstractions.Services/IPlayerStats.md). |
| interface [IEntityPortrait](./Stellar.Abstractions.Services/IEntityPortrait.md) | Live, posed 3D portrait of a player entity — full body, real outfit, idle-animated — rendered by the game's own UI-model pipeline into a render texture that a [`RenderTextureHostElement`](./Stellar.Abstractions.Services/RenderTextureHostElement.md) displays. Works for the local player and any other player (the subject is created from the player's social data, the same way the game's own character preview windows do it). The plugin calls [`Show`](./Stellar.Abstractions.Services/IEntityPortrait/Show.md) when its portrait slot opens on an entity and reads [`Texture`](./Stellar.Abstractions.Services/IEntityPortrait/Texture.md) (the boxed `UnityEngine.Texture`) to display it. |
| interface [IFramework](./Stellar.Abstractions.Services/IFramework.md) | Per-frame callbacks driven by the game's main update loop. |
| interface [IGameAssets](./Stellar.Abstractions.Services/IGameAssets.md) | Toolkit service for loading game assets (textures, sprites) via the game's own Addressables loader (`ZResLoader`). Provides an opaque texture handle so plugins can render icons without a direct Unity dependency on `Texture2D`. |
| interface [IGameData](./Stellar.Abstractions.Services/IGameData.md) | Read-only lookup over the game's static `Panda.Table` data. Resolves Bokura.*TableBase row IDs to typed POCO records with display strings pre-resolved through the game's LocalizationMgr. Eager batch (Skill, Buff, Profession, Attribute, Item) is loaded inline at HybridCLR-ready; [`IsAvailable`](./Stellar.Abstractions.Services/IGameData/IsAvailable.md) becomes true once that completes. 17 deferred tables drain one per Game.Update tick — lookups return null until their table is loaded (typically a few seconds after IsAvailable=true). Strings are resolved at cache-build time. Mid-run language switch will not refresh names until next game restart (deferred to Phase 6+). |
| interface [IGameDataCombat](./Stellar.Abstractions.Services/IGameDataCombat.md) | Static-data lookups for combat-related rows. |
| interface [IGameDataEquip](./Stellar.Abstractions.Services/IGameDataEquip.md) | Static-data lookups for the gear-slot tables (equip rows, attr-lib ranges, slot names). Deferred-loaded: lookups return null/empty for a few seconds after boot. |
| interface [IGameDataInventory](./Stellar.Abstractions.Services/IGameDataInventory.md) | Static-data lookups for inventory-related rows. |
| interface [IGameDataProgress](./Stellar.Abstractions.Services/IGameDataProgress.md) | Static-data lookups for progression-related rows. |
| interface [IGameDataResonance](./Stellar.Abstractions.Services/IGameDataResonance.md) | Static game-data lookups for Battle Imagines (Resonance Skills). |
| interface [IGameDataWorld](./Stellar.Abstractions.Services/IGameDataWorld.md) | Static-data lookups for world-related rows. |
| interface [IGameEvents](./Stellar.Abstractions.Services/IGameEvents.md) | Subscribe to typed game events by their fully-qualified Panda type name. Backed by MessagePipe's `ISubscriber<T>` when the host can reach the game's container; otherwise backed by hook fallbacks. Plugins see the same API either way. |
| interface [IHotkeys](./Stellar.Abstractions.Services/IHotkeys.md) | Plugin-facing hotkey service. Declare bindable keyboard actions and receive callbacks when pressed. |
| interface [IHudHandle](./Stellar.Abstractions.Services/IHudHandle.md) | Handle to a registered HUD. Auto-removed on plugin/framework dispose. |
| interface [IHudHost](./Stellar.Abstractions.Services/IHudHost.md) | Plugin-facing toolkit: describe a HUD as composed [`HudElement`](./Stellar.Abstractions.Services/HudElement.md)s; the framework builds native uGUI and owns rendering, lifecycle, refresh, and animation. |
| interface [IInventory](./Stellar.Abstractions.Services/IInventory.md) | Read-only access to the player's module-package inventory and currently equipped set. Sampled at 1Hz from the Game.Update tick; not subscription driven (inventory changes are infrequent enough that 1Hz polling beats the bookkeeping cost of subscriptions). Pair with [`GetAttributeProfile`](./Stellar.Abstractions.Services/IGameDataCombat/GetAttributeProfile.md) to resolve `ModulePart.AttrId` labels (Phase 6 reuse). Threading: [`GetModules`](./Stellar.Abstractions.Services/IInventory/GetModules.md) and [`GetEquipped`](./Stellar.Abstractions.Services/IInventory/GetEquipped.md) are thread-safe lock-free reads (Volatile under the hood). The [`InventoryChanged`](./Stellar.Abstractions.Services/IInventory/InventoryChanged.md) event raises on the framework Update thread; subscribers can call IMGUI from the handler. |
| interface [ILauncher](./Stellar.Abstractions.Services/ILauncher.md) | Registry of launcher entries. Plugins call [`Register`](./Stellar.Abstractions.Services/ILauncher/Register.md) to add a tile to the Stellar launcher menu and hold the returned IDisposable to remove it on unload. [`Entries`](./Stellar.Abstractions.Services/ILauncher/Entries.md) preserves registration order. |
| record [ImageElement](./Stellar.Abstractions.Services/ImageElement.md) | Escape hatch: plugin supplies its own PNG; framework displays it. Consistency is the plugin's responsibility here (the one unenforced spot). |
| interface [IModuleEquip](./Stellar.Abstractions.Services/IModuleEquip.md) | Game-state mutation primitive: equip / uninstall modules via the game's own RPC dispatcher. Implementations invoke the game's Lua functions `ModVM.AsyncEquipMod` / `ModVM.AsyncUninstallMod` through the `ZLuaFramework` bridge — the plugin supplies inputs, the game's Lua code builds the protobuf and applies its own validation. User-initiated only. Plugins MUST trigger calls from a user action (button press, slash command, hotkey) — not from a timer, scheduled event, or background loop. See CLAUDE.md § "Out of scope". Async: every call polls the game's `Mod.ModSlots` map until it reflects the requested change, or times out at 6 seconds (matching the Lua proxy timeout). Callers must `await` and check the returned [`EquipResult`](./Stellar.Abstractions.Domain.Inventory/EquipResult.md). |
| interface [INamedTheme](./Stellar.Abstractions.Services/INamedTheme.md) | User-facing theme controls — preset selector + global font scale. The concrete implementation persists selections to config and tints every plugin's rendered chrome / text on the next OnGUI pass. |
| interface [INativeUiElementHandle](./Stellar.Abstractions.Services/INativeUiElementHandle.md) | Handle to a registered element. Auto-removed on plugin/framework dispose. |
| interface [INativeUiHost](./Stellar.Abstractions.Services/INativeUiHost.md) | Plugin-facing service for injecting declarative mod uGUI into curated game-UI anchors. The framework builds, styles, and lifecycle-manages the real uGUI; plugins only describe intent via [`NativeUiElementSpec`](./Stellar.Abstractions.Services/NativeUiElementSpec.md). |
| record [IndicatorSpec](./Stellar.Abstractions.Services/IndicatorSpec.md) | A read-only text indicator; *OnUpdate* is re-pulled each refresh. |
| record [InputElement](./Stellar.Abstractions.Services/InputElement.md) | Single-line text field (wraps the proven UGuiTextInput: Enter submits without opening chat, Esc/cursor escape). *Get* seeds the text; Enter/blur calls *Submit*. *OnChange* (optional) fires per-keystroke — use it for live filters that should reflow as-you-type rather than on Enter. |
| interface [IPartyControl](./Stellar.Abstractions.Services/IPartyControl.md) | Plugin-facing control over the local player's party. Actions are routed through the game's own dispatcher (never hand-built packets); the game applies its own validation, so a request may be silently rejected (e.g. a non-leader, or the 20-player mode being locked for the account). |
| interface [IPartyEvents](./Stellar.Abstractions.Services/IPartyEvents.md) | Party membership lifecycle events. All events fire on the Unity main thread. |
| interface [IPartyRoster](./Stellar.Abstractions.Services/IPartyRoster.md) | Read-only access to the current party roster. Observed on the Unity main thread. |
| interface [IPartySnapshot](./Stellar.Abstractions.Services/IPartySnapshot.md) | Read-only scalar state of the local player's party. All properties are observed on the Unity main thread. |
| interface [IPlayerIdentity](./Stellar.Abstractions.Services/IPlayerIdentity.md) | Identity and availability facet of the local player's state. |
| interface [IPlayerLocation](./Stellar.Abstractions.Services/IPlayerLocation.md) | Location facet of the local player's state. |
| interface [IPlayerState](./Stellar.Abstractions.Services/IPlayerState.md) | Read-only view of the local player's basic state. All properties are safe to read at any time; when [`IsAvailable`](./Stellar.Abstractions.Services/IPlayerIdentity/IsAvailable.md) is `false` (e.g. on title / character select / loading screens) the other properties return defaults (empty string, zero, [`Zero`](./Stellar.Abstractions.Domain/Position3D/Zero.md)). |
| interface [IPlayerStats](./Stellar.Abstractions.Services/IPlayerStats.md) | Read-only access to live player attribute values. Each tracked attribute is identified by its `Zproto.EAttrType` integer code (e.g. 11011 for `AttrStrengthTotal`). Pair with [`GetAttribute`](./Stellar.Abstractions.Services/IGameDataCombat/GetAttribute.md) to resolve the localized label and [`GetAttributeProfile`](./Stellar.Abstractions.Services/IGameDataCombat/GetAttributeProfile.md) to discover the UI group. Attributes must be [`Subscribe`](./Stellar.Abstractions.Services/IPlayerStats/Subscribe.md)d before they are sampled. The probe polls only subscribed IDs each `Game.Update` tick — sampling all 1289 EAttrType members per frame would be wasteful. Unsubscribed IDs return null. |
| interface [IPlayerVitals](./Stellar.Abstractions.Services/IPlayerVitals.md) | Vitals (HP / Stamina) facet of the local player's state. |
| interface [IPluginConfig](./Stellar.Abstractions.Services/IPluginConfig.md) | Per-plugin persistent configuration. Each plugin gets one JSON file on disk (`<plugin-dir>/<pluginGuid>.config.json`) organized into named sections. Plugins read settings on construct, write whenever the user changes them, and subscribe to [`SectionChanged`](./Stellar.Abstractions.Services/IPluginConfig/SectionChanged.md) to react to external edits or settings-window writes from sibling code. |
| interface [IPluginInventory](./Stellar.Abstractions.Services/IPluginInventory.md) | Read-side surface for the Settings → Plugins panel. Lists every discovered plugin with its current enabled / errored state, fires on state changes, and lets the user request a recovery retry after an exception. |
| interface [IPluginLog](./Stellar.Abstractions.Services/IPluginLog.md) | Plugin-scoped logger. Output is routed through the host's log sink. |
| interface [IPluginServices](./Stellar.Abstractions.Services/IPluginServices.md) | The single object passed to every plugin's constructor. Plugins obtain all framework capabilities by reading sub-services from here. |
| interface [IProfileCardActions](./Stellar.Abstractions.Services/IProfileCardActions.md) | Plugin-facing service for contributing buttons to the game's native profile card action bar. Each registered [`ProfileCardActionSpec`](./Stellar.Abstractions.Services/ProfileCardActionSpec.md) becomes a styled button the framework injects into the card on open; the returned handle removes the action when disposed. |
| interface [IResonanceState](./Stellar.Abstractions.Services/IResonanceState.md) | Read-only view of the local player's equipped Battle Imagines (Resonance Skills), decoded from `CharSerialize.resonance` (wire field 28). The equipped set drives the CombatMeter's trailing Imagine icons for the self row. Populated on the network receive thread and read on the main thread; implementations publish an immutable snapshot so reads are lock-free. |
| interface [ITheme](./Stellar.Abstractions.Services/ITheme.md) | Plugin-facing theme façade. The single entry point plugins receive via `IPluginServices.Theme`; bundles the palette, draw helpers, semantic text, and layout primitives under one type. Facade-inheritance keeps the surface cohesive while letting each facet sit under the analyzer's member-count cap. |
| interface [IThemeAccess](./Stellar.Abstractions.Services/IThemeAccess.md) | Composition + access facet — accessors for the semantic text helpers and plugin colour registry. |
| interface [IThemeBaseColors](./Stellar.Abstractions.Services/IThemeBaseColors.md) | Original Phase 8 colour vocabulary — preserved verbatim. Extracted into its own facet by Phase 9b so [`IThemeColors`](./Stellar.Abstractions.Services/IThemeColors.md) can remain at the STELLAR0005 8-member cap while gaining HUD + menu colour sub-facets. |
| interface [IThemeColors](./Stellar.Abstractions.Services/IThemeColors.md) | Theme colour facade — zero declared members. Plugins access the full colour vocabulary (base, HUD, menu) through this single type; the split into [`IThemeBaseColors`](./Stellar.Abstractions.Services/IThemeBaseColors.md) + [`IThemeHudColors`](./Stellar.Abstractions.Services/IThemeHudColors.md) + [`IThemeMenuColors`](./Stellar.Abstractions.Services/IThemeMenuColors.md) exists so each facet stays inside the STELLAR0005 8-member cap (the analyzer counts declared members per interface; inherited members are unbounded). |
| interface [IThemeHudColors](./Stellar.Abstractions.Services/IThemeHudColors.md) | Phase 9b HUD-overlay palette — colours used by `WindowPanelStyle.HudOverlay` chrome and the `PillStatus` chip. Renders on top of the live game world, so all colours assume no enclosing panel. |
| interface [IThemeMenuColors](./Stellar.Abstractions.Services/IThemeMenuColors.md) | Phase 9b menu / tool-panel palette — colours used by `WindowPanelStyle.GlassMenu` chrome. Renders as a light gradient panel with subtle border and shadow, matching the game's Profile / Modules visual language. |
| interface [IThemePalette](./Stellar.Abstractions.Services/IThemePalette.md) | Palette + font identity facet of the active theme. |
| interface [IThemeText](./Stellar.Abstractions.Services/IThemeText.md) | Semantic text drawing API for plugins, modelled after HTML's heading + paragraph levels. Each method renders text at the corresponding base size and weight, scaled by [`FontScale`](./Stellar.Abstractions.Services/IThemeText/FontScale.md) so a single Phase 9 user setting can globally enlarge or shrink every plugin's text in one place. Plugin usage: _services.Theme.Text.DrawH2("Combat Meter"); _services.Theme.Text.DrawBody($"DPS: {dps:F1}"); _services.Theme.Text.DrawCaption("(last update 2s ago)"); Base sizes match the mockup hierarchy (theme-palette-v3 / panel-styles): H1 = 20px (top-level section), H2 = 17px (subheading), H3 = 15px (group label), H4 = 14px (list item header), Body = 13px (paragraph default), Caption = 11px (muted footnote). All headings render in bold; Body and Caption render in normal weight. Captions use the muted text colour. |
| interface [IWindowControl](./Stellar.Abstractions.Services/IWindowControl.md) | Handle to a registered interactive uGUI window. Auto-removed on plugin/framework dispose. |
| interface [IWindowHost](./Stellar.Abstractions.Services/IWindowHost.md) | Plugin/framework-facing toolkit: describe an interactive window as composed elements; the framework builds native uGUI and owns chrome, rendering, refresh, lifecycle, input gating, persistence. |
| record [LabelWidget](./Stellar.Abstractions.Services/LabelWidget.md) | Static text label child widget for a [`PanelSpec`](./Stellar.Abstractions.Services/PanelSpec.md). |
| record [LauncherEntry](./Stellar.Abstractions.Services/LauncherEntry.md) | A plugin (or the framework) entry shown in the Stellar launcher menu. *Title* doubles as the stable identity used to persist the entry's pinned state, so keep it unique and stable across sessions. *IconPng* (raw PNG bytes) is the preferred icon — it rasterises to a native-looking sprite; *IconKey* is the font-glyph fallback. *OnOpen* runs when the user clicks the tile. |
| enum [LauncherGroup](./Stellar.Abstractions.Services/LauncherGroup.md) | Which Full-mode section an entry sits in. |
| enum [LauncherMode](./Stellar.Abstractions.Services/LauncherMode.md) | How the launcher menu lays itself out. |
| record [ListElement](./Stellar.Abstractions.Services/ListElement.md) | Variable-length list, bounded by Slots.Count. All slots built once; the first *VisibleCount*() are SetActive-shown each refresh. *Columns*&gt;1 grids them. *CellWidth*/*CellHeight* (when &gt; 0, multi-column only) override the grid's default cell size — use to widen columns past the framework default (e.g. the StatInspector mini-HUD, whose icon+label+value row needs more than the default cell width). |
| record [MenuButtonSpec](./Stellar.Abstractions.Services/MenuButtonSpec.md) | A native-styled button injected at *Anchor*. |
| struct [MeterRowData](./Stellar.Abstractions.Services/MeterRowData.md) | All data one CombatMeter row needs to paint, in framework-neutral types (no UnityEngine). The plugin computes this from its services each refresh; the framework's `BuildMeterRow` reproduces the bespoke borderless meter row (HP spine, class crest, name·spec·share line, role-coloured metric bar with the per-second/total overlay, self highlight, offline scrim). A plain field struct (no big constructor) so it stays clear of the analyzer's parameter/ctor-dependency caps and mirrors the plugin's former MeterRowVisual. |
| record [MeterRowElement](./Stellar.Abstractions.Services/MeterRowElement.md) | One bespoke CombatMeter row — the borderless, role-coloured, animated meter row (HP spine + class crest + name·spec·share line + role-coloured metric bar with a per-second/total overlay + self highlight + offline scrim). The framework reproduces the custom visual that the IMGUI `MeterRowView` drew, so the meter keeps its distinct look (it does NOT ride the generic Row/Bar primitives). *Data* is re-pulled on the window's capped refresh (poll-diffed) — the plugin snapshots its per-combatant state so the Func never allocates. Fixed 48-px row height; width fills the row. Compose inside a [`ListElement`](./Stellar.Abstractions.Services/ListElement.md). |
| abstract record [NativeUiElementSpec](./Stellar.Abstractions.Services/NativeUiElementSpec.md) | Base for a declarative mod-uGUI element. Unity-free so it lives in Abstractions. |
| record [PanelSpec](./Stellar.Abstractions.Services/PanelSpec.md) | A themed panel containing declarative child widgets. |
| abstract record [PanelWidget](./Stellar.Abstractions.Services/PanelWidget.md) | Declarative panel child widgets. |
| record [PillElement](./Stellar.Abstractions.Services/PillElement.md) | Rounded pill badge with dynamic text and optional tint colour. Suitable for short status labels (e.g. "Offline", rank numbers). |
| record [ProfileCardActionSpec](./Stellar.Abstractions.Services/ProfileCardActionSpec.md) | A button a plugin contributes to the game's native profile card action bar. The framework injects a styled button per registered spec into the card's action row (icon over label, matching the native action buttons) and, on click, resolves the carded player and invokes *OnClick* with their [`EntityId`](./Stellar.Abstractions.Domain/EntityId.md). |
| record [RenderTextureHostElement](./Stellar.Abstractions.Services/RenderTextureHostElement.md) | Hosts a live render texture in a fixed-size box (e.g. the Entity Inspector's 3D portrait). The framework binds the boxed `UnityEngine.Texture` returned by *Texture* onto a uGUI RawImage and re-pulls it each frame, so a texture created after the window builds still appears. Renderer-neutral — the texture crosses the boundary as Object so Abstractions names no Unity type; null = blank box. |
| record [RowElement](./Stellar.Abstractions.Services/RowElement.md) | Horizontal layout container: children arranged left-to-right with optional spacing. |
| record [ScrollElement](./Stellar.Abstractions.Services/ScrollElement.md) | Vertical scroll viewport (fixed *Height*) wrapping a child subtree + a themed thin scrollbar. |
| record [SelectableElement](./Stellar.Abstractions.Services/SelectableElement.md) | Makes any *Child* subtree clickable as a whole and tints its background by interaction state — the rich-row analog of [`ButtonElement`](./Stellar.Abstractions.Services/ButtonElement.md) (which only wraps a single label). For list rows that are multi-line / multi-widget (a history session, a recent-lookup entry) where a per-row button would lose the layout. Rest = transparent; hover = faint accent wash; *Selected*() true (poll-diffed) = a stronger accent fill. A click anywhere on the row fires *OnClick*. Composes inside a List/Column like any leaf. |
| record [SeparatorElement](./Stellar.Abstractions.Services/SeparatorElement.md) | 1 px themed divider (the faint row/section separator). *Vertical* = a 1 px-wide full-height divider for splitting columns inside a Row (else a 1 px-tall full-width line between rows). |
| record [SliderElement](./Stellar.Abstractions.Services/SliderElement.md) | Drag slider over [*Min*,*Max*]. *Get* reflects external state (poll-diffed); a drag calls *Set*. |
| struct [SocialIdentity](./Stellar.Abstractions.Services/SocialIdentity.md) | Affiliation/prestige extras from a full-mask social reply (the ID-card fetch requests mask 0 = all sections). Thin-mask replies (nameplate/avatar queries carry identity only) leave these at defaults — values are best-effort, last-reply-wins via the cache. |
| record [SocialSnapshot](./Stellar.Abstractions.Services/SocialSnapshot.md) | A player's on-demand social-data reply (the game's own `Social.GetSocialData` RPC, which the inspector triggers via the portrait path). Available for ANY player regardless of AOI proximity — the fallback source the inspector uses when the proximity broadcast is absent. Carries identity, ability score (fight point), profession, gear-by-slot, worn cosmetics and affiliation extras; NOT the secondary-stat breakdown or skills (those are AOI-broadcast only — see the design spec §2). |
| record [SpacerElement](./Stellar.Abstractions.Services/SpacerElement.md) | Gap inside a Row. *Width* = 0 → flexible (expands to push following siblings to the far edge); &gt; 0 → a fixed-width spacer (e.g. to balance a right-aligned control so a centred element is TRULY centred, not centred-minus-that-control). *Height* &gt; 0 → a fixed-height gap (use in a Column for a little vertical margin between sections, no divider line). |
| record [SpriteElement](./Stellar.Abstractions.Services/SpriteElement.md) | Sub-rect of a packed atlas PNG — the `DrawTextureWithTexCoords` analog. *Atlas* is the whole packed sheet (loaded once, mipmap-smoothed); *Uv* is the normalized sub-rect to show (x,y,w,h in 0..1, origin BOTTOM-left per Unity texture space); *Width*/*Height* are the on-screen pixel size. Use for icon atlases (e.g. a stat-icon sheet) where [`ImageElement`](./Stellar.Abstractions.Services/ImageElement.md)'s whole-PNG display can't pick one glyph. *Atlas* is poll-friendly (re-read each build). *UvFunc* (optional) makes the sub-rect DYNAMIC — re-pulled on the window refresh so one pooled/recycled slot can show a different atlas cell as its backing data changes (e.g. a stat-icon keyed to the attribute the row currently represents). When null, *Uv* is static (set once at build) — use that for a fixed icon (e.g. a gear). |
| record [SwatchElement](./Stellar.Abstractions.Services/SwatchElement.md) | Solid-colour box (the theme-editor colour swatch). *Color* is poll-diffed so it tracks live edits. |
| enum [TextAlign](./Stellar.Abstractions.Services/TextAlign.md) | Horizontal text alignment within the text's cell. Default Left; Right is used for numeric table columns (so magnitudes line up against the right edge of a fixed-width [`CellElement`](./Stellar.Abstractions.Services/CellElement.md)). |
| record [TextElement](./Stellar.Abstractions.Services/TextElement.md) | Themed text. *Color* Func null (or returns null) = framework default; a Func lets colour animate per-refresh (e.g. delta-flash). *Width* &gt; 0 fixes the cell width (the text wraps within it) — use to form aligned columns (e.g. a plugin-name column so the version after it starts at a consistent x). *Align* sets horizontal alignment (Right for numeric columns). *Shadow* draws a dark outline behind the glyphs — for chrome-less overlays (a borderless HUD with no background) where light text must stay legible over arbitrary world backgrounds. |
| record [TileElement](./Stellar.Abstractions.Services/TileElement.md) | Icon tile (launcher): a centred PNG *Icon* over an optional *Label*, no background. Hover brightens icon+label and grows the icon ~1.18× (the native rail feel). The whole tile clicks via *OnClick*. When *Pinned* != null, a ★/☆ toggle overlays the top-right corner (its own click → *OnTogglePin*). *Label* null ⇒ icon-only (title-bar mode/rotate toggles); a non-null Func ALWAYS builds the label cell (so a live label that is empty at build time — e.g. a plugin that registers later — still shows its name once available). |
| record [ToggleElement](./Stellar.Abstractions.Services/ToggleElement.md) | Two-way toggle. *Get* reflects external state (poll-diffed); a click calls *Set* with the new value. |
| record [ValueRowWidget](./Stellar.Abstractions.Services/ValueRowWidget.md) | Key/value row child widget for a [`PanelSpec`](./Stellar.Abstractions.Services/PanelSpec.md); value is re-pulled each refresh. |
| record [VirtualListElement](./Stellar.Abstractions.Services/VirtualListElement.md) | Scroll-windowed variable list: renders a small fixed *Pool* of K rows (K ≈ viewport rows + a few margin rows) over a logical list of *Count*() items that may be FAR larger than K. The framework sizes a scroll content spacer to Count()*RowHeight, tracks the scroll offset, computes the first visible logical index, calls *OnWindow*(first) BEFORE pulling any slot values (so the plugin's row Funcs resolve item first+slotIndex), positions the pool rows at their logical offset, and SetActive-shows only rows whose logical index &lt; Count(). Rows MUST be uniform fixed-height (*RowHeight*). Use for large pickers; the small [`ListElement`](./Stellar.Abstractions.Services/ListElement.md) (eager full pool) stays for short lists. *Height* is the viewport height. |
| static class [VirtualListMath](./Stellar.Abstractions.Services/VirtualListMath.md) | Pure scroll-windowing math for [`VirtualListElement`](./Stellar.Abstractions.Services/VirtualListElement.md) (BCL only, unit-tested on CI). The renderer maps a ScrollRect's content offset to the first logical row to bind. |
| record [WindowRegistration](./Stellar.Abstractions.Services/WindowRegistration.md) | A registered interactive window: its spec + the root element to render. *TitleLeading* (when set) replaces the title-bar text (e.g. a logo + wordmark row); *TitleTrailing* is built right-aligned before the ✕ (e.g. mode/rotate toggles). Both null ⇒ the chrome draws the plain Title text. *OnClose* (when set) is invoked by the ✕ instead of just hiding the GameObject — wire it to the window's [`SetVisible`](./Stellar.Abstractions.Services/IWindowControl/SetVisible.md)(false) so [`IsShown`](./Stellar.Abstractions.Services/IWindowControl/IsShown.md) stays in sync (otherwise a rail/hotkey toggle needs two presses to reopen after a ✕ close). |

<!-- DO NOT EDIT: generated by xmldocmd for Stellar.Abstractions.dll -->
